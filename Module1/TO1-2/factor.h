#ifndef FACTOR_H_
#define FACTOR_H_

//can use #pragma once if that has been loaded

//Prototypes
void FactorMod3();

//Added another requirement for flexible input, not hard coded
void FactorModX(int mod_number); //we are now passing something in

void FactorModXRange(int mod_number, int max_range);


#endif /* !FACTOR_H_ */
